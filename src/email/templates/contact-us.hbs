<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Contact Form Submission</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            padding: 20px;
            border: 1px solid #ddd;
            max-width: 600px;
            margin: 0 auto;
        }
        h2 {
            color: #0056b3;
        }
        .field-label {
            font-weight: bold;
            color: #333;
        }
        .field-value {
            margin-bottom: 10px;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>New Contact Form Submission</h2>
        
        <p><span class="field-label">First Name:</span> <span class="field-value">{{firstName}}</span></p>
        <p><span class="field-label">Last Name:</span> <span class="field-value">{{lastName}}</span></p>
        <p><span class="field-label">Email:</span> <span class="field-value">{{email}}</span></p>
        <p><span class="field-label">Phone Number:</span> <span class="field-value">{{phoneNumber}}</span></p>
        
        <h3>Message</h3>
        <p>{{message}}</p>

        <hr />
        <p>This message was sent from your website's contact form.</p>
    </div>
</body>
</html>
